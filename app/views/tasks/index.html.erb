<h1>タスク一覧</h1>

<table>
  <tr>
    <th>ID</th>
    <th>タスク名</th>
    <th>タスクの説明</th>
  </tr>
  <% @tasks.each do |tasks| %>
  <tr>
    <td><%= tasks.id %></td>
    <td><%= tasks.name %></td>
    <td><%= tasks.description %></td>
  </tr>
  <% end %>
</table>